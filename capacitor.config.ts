import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'poc-new-feature-login',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
